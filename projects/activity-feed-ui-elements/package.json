{
  "name": "activity-feed-ui-elements",
  "version": "1.0.0",
  "description": "",
  "main": "karma.conf.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "hello": "echo 999",
    "build:ui-components": "ng build --prod --project ui-components",
    "build:ui": "ng build --prod --project ui",
    "build:activity-feed-ui-elements": "ng build --prod --project activity-feed-ui-elements --output-hashing none && npm run build:files",
    "build:files": "node build-files.js"
  },
  "author": "",
  "license": "ISC",
  "files": [
    "activity-feed-ui-elements.js",
    "styles.scss"
  ]
}
